[project]
name = "uv-fastapi-click"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dev = "fastapi dev"

[tool.uv.workspace]
members = ["packages/api", "packages/cli"]
